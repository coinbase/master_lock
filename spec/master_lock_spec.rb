require "spec_helper"

describe MasterLock do
  it "has a version number" do
    expect(MasterLock::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
